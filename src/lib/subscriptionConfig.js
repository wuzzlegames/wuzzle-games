/**
 * When true: subscription CTAs and paywalls are shown; locked archives, advanced stats, etc. require subscription.
 * When false: everything is unlocked and no subscription UI or copy is shown (flip to true later to re-enable).
 */
export const isSubscriptionAllowed = false;
